# IDE / editor
.idea/

# OS
.DS_Store

# Defensive: real config + secrets (TARGET_REPO, CLAUDE_CODE_OAUTH_TOKEN, GIT_PAT)
# live in a target directory outside this repo, never here. The committed template
# is setup/target-template/.env.example.
.env
